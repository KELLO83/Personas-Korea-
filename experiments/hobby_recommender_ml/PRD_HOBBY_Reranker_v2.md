# Historical PRD: Hobby Reranker v2

This document is retained as a historical reference. The active experiment contract is `PRD.md` in this folder.

## Historical Scope

The v2 plan introduced learned reranking, negative-sampling ablations, text-embedding controls, and diversity experiments.

## Current Status

The current documented default is the E5-small-ko-v2 domain-specific LightGBM Stage 2 ranker recorded in `artifacts/experiment_decisions.json`. Any future default change must update that decision log first.

